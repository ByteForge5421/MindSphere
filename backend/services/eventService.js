/**
 * Event tracking service
 */

function trackEvent(userId, eventType, metadata = {}) {
  // TODO: Implement event tracking
  console.log(`Event tracked: user ${userId}, type: ${eventType}`);
}

module.exports = {
  trackEvent
};
